import React from 'react'

// watch for changes in store
// on store change unlock itself for clicks
// onClick request headlines and dispatch them
// after successful dispatch lock itself and wait for future updates

const SearchButton: React.FC = () => {
  return (
    <div>I load this shoit</div>
  )
}
